<div class="container">
  <div class="row">
    <div class="col-xs-4 col-xs-offset-3">

      <% @articles.each do |article| %>
      <h2><%= link_to article.name.capitalize, article_path(article) %></h2>
      <p><%= article.details %></p>
      <%= link_to "Delete", article, method: :delete%>

      <% end %>
      <br>
      <br>
      <h2><%= link_to "create new!", new_article_path%></h2>
      <br>
      <br>

    </div>
  </div>
</div>
